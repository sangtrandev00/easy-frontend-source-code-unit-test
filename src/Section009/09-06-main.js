const numberList = [1, 3, 5, 7];
numberList.pop();
const lastElement = numberList.pop(); // return the last element is removed by this function
const firstElement = numberList.shift(); // return the first  element is removed by this function

console.log(numberList);
console.log(lastElement);
console.log(firstElement);
